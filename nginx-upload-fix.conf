# Add this to your nginx server block or http block
# Increases upload size limit to 50MB for screenshots

client_max_body_size 50M;
client_body_buffer_size 50M;

# If you need to add this to your nginx config, run:
# echo "client_max_body_size 50M;" >> /etc/nginx/sites-available/YOUR_SITE_CONFIG
# nginx -t && systemctl reload nginx